revision: "followup-2"
description: >-
  Replacement for the operationally aborted followup-1 exposure, using the
  same balanced coverage design with hardened local-model parsing.
stages:
  - name: extraction
    episodes_per_task: 4
    tasks:
      - iron_ore_throughput
  - name: processing
    episodes_per_task: 4
    tasks:
      - iron_plate_throughput
held_out_tasks:
  - iron_gear_wheel_throughput
transfer_tasks: []
benchmark_arms:
  - baseline
  - domain_only
  - warm_positive
  - warm_diagnostic
